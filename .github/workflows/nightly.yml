name: Publish Nightly

on:
    workflow_run:
        workflows: ["Build & Release"]
        types: [completed]
        branches: [main]

permissions:
    actions: read
    contents: write

jobs:
    publish:
        if: >-
            github.event.workflow_run.conclusion == 'success' &&
            github.event.workflow_run.event == 'push'
        runs-on: ubuntu-latest

        steps:
            - name: Check out the built commit
              uses: actions/checkout@v6
              with:
                  ref: ${{ github.event.workflow_run.head_sha }}
                  fetch-depth: 0

            - name: Determine nightly version
              id: metadata
              shell: bash
              run: echo "timestamp=$(git show -s --format=%ct HEAD)" >> "$GITHUB_OUTPUT"

            - name: Download build artifacts
              uses: actions/download-artifact@v8
              with:
                  run-id: ${{ github.event.workflow_run.id }}
                  github-token: ${{ github.token }}
                  repository: ${{ github.repository }}
                  pattern: yeet-*
                  merge-multiple: true
                  path: ${{ runner.temp }}/nightly-artifacts

            - name: Publish nightly pre-release
              env:
                  ARTIFACT_DIR: ${{ runner.temp }}/nightly-artifacts
                  BUILD_TIMESTAMP: ${{ steps.metadata.outputs.timestamp }}
                  GH_TOKEN: ${{ github.token }}
                  NIGHTLY_TAG: nightly-${{ steps.metadata.outputs.timestamp }}
                  SOURCE_BRANCH: ${{ github.event.workflow_run.head_branch }}
                  SOURCE_SHA: ${{ github.event.workflow_run.head_sha }}
                  SOURCE_URL: ${{ github.server_url }}/${{ github.repository }}/commit/${{ github.event.workflow_run.head_sha }}
              shell: bash
              run: |
                  if gh release view "$NIGHTLY_TAG" >/dev/null 2>&1; then
                    gh release upload "$NIGHTLY_TAG" "$ARTIFACT_DIR"/* --clobber
                    gh release edit "$NIGHTLY_TAG" \
                      --title "Autobuild: $BUILD_TIMESTAMP" \
                      --notes "Automated build from commit [$SOURCE_SHA]($SOURCE_URL) on $SOURCE_BRANCH" \
                      --prerelease
                  else
                    gh release create "$NIGHTLY_TAG" "$ARTIFACT_DIR"/* \
                      --target "$SOURCE_SHA" \
                      --title "Autobuild: $BUILD_TIMESTAMP" \
                      --notes "Automated build from commit [$SOURCE_SHA]($SOURCE_URL) on $SOURCE_BRANCH" \
                      --prerelease \
                      --latest=false
                  fi
